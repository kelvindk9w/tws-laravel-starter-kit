Versão em texto puro dos e-mails.

Não há um arquivo por e-mail aqui de propósito: `auto.blade.php` imprime o
texto gerado a partir do HTML da própria mensagem (`App\Core\Mail\PlainText`).
Duas cópias do mesmo texto sempre acabam divergindo — esta não pode.
