<?php

declare(strict_types=1);

// Audit trail messages (en) — the `audit:prune` command.

return [

    'prune_disabled' => 'Audit trail pruning is disabled (AUDIT_RETENTION_DAYS=0).',
    'pruned' => ':count audit event(s) older than :days days removed.',

];
